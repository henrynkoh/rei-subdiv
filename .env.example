# Floating “GitHub” button on the landing page (full repo URL)
NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/henrynkoh/rei-subdiv

# Optional: GitHub Pages project site (no trailing slash), then build with:
# BASE_PATH=/rei-subdiv STATIC_EXPORT=1 npm run build
# BASE_PATH=/rei-subdiv
